# Server
HOST=0.0.0.0
PORT=1337
NODE_ENV=production

# Secrets
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified
ENCRYPTION_KEY=tobemodified

# Database (Railway PostgreSQL or external)
DATABASE_CLIENT=postgres
DATABASE_URL=postgresql://username:password@hostname:port/database_name
DATABASE_HOST=hostname
DATABASE_PORT=5432
DATABASE_NAME=ensiklopedia_doa
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=your-database-password
DATABASE_SSL=true

# File Upload Configuration (Railway provides file system storage)
# For production file storage, you can use:
# - Cloudinary (recommended for images)
# - Railway Volume storage
# - Or external services like AWS S3, Google Cloud Storage
